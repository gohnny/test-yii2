<div class="blog">
    <div class="container">

        <h1>Формирование запросов для выборки из БД в yii2</h1>
        Сборник методов для формирования запросов к БД в yii2.
        Здесь я привел список с функциями, которые будут наиболее полезными для выборки (и не только) данные из базы
        данных в yii2.
        Рассмотрены примеры построения запросов для выборки из базы с использованием ActiveRecord и yii\db\ActiveQuery
        Каждый метод (функция) представлена в виде ссылки на якорь на этой же странице с более подробным разъяснением и
        практическим примером.

        Вот всё, что мы имеем в арсенале, ну или почти всё:
        findOne()
        findAll()
        find()
        findBySql()

        select()
        addSelect()
        selectOption() where()
        andWhere()
        orWhere()

        createCommand()
        addParams()
        prepare()

        join()
        innerJoin()
        leftJoin()
        rightJoin()
        union()

        joinWith()
        innerJoinWith()
        with() count()
        sum()
        average()
        min()
        max()
        scalar()

        distinct()


        onCondition()
        orOnCondition()
        andOnCondition() andFilterCompare()
        filterHaving()
        filterWhere() column()
        exists()
        each() orderBy()
        addOrderBy()
        addGroupBy()
        groupBy()
        having()
        andHaving()
        indexBy()
        limit()
        offset()
        Примеры использования методов.
    </div>
</div>